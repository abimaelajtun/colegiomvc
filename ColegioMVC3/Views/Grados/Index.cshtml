@model IEnumerable<ColegioMVC3.Models.Grado>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Profesor
        </th>
        <th>
            Grado
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Profesor.Nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GradoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GradoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GradoId })
        </td>
    </tr>
}

</table>
